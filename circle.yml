machine:
  node:
    version: 7.10.0
dependencies:
  override:
    - npm install
    - npm install -g webpack
deployment:
  heroku:
    branch: master
    commands:
      - git config --global user.email "jksemacio@gmail.com"
      - git config --global user.name "jksemacio"
      - git pull git@heroku.com:semu.git master
      - npm run build
      - git add build/
      - git commit -m "added"
      - git push git@heroku.com:semu.git master