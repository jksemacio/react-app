machine:
  node:
    version: 7.10.0
dependencies:
  override:
    - npm install
    - npm install -g webpack
    - npm run build
    - git config user.email "jksemacio@gmail.com"
    - git config user.name "jksemacio"
    - git add build/
    - git commit -am "added"
deployment:
  heroku:
    branch: master
    heroku:
      appname: semu