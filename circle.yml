machine:
  node:
    version: 7.10.0
dependencies:
  override:
    - npm install
    - npm install -g webpack
    - npm run build
    - git add build/
deployment:
  heroku:
    branch: master
    heroku:
      appname: semu