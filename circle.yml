machine:
  node:
    version: 7.10.0
dependencies:
  override:
    - npm install
    - npm install webpack -g
    - npm run build
deployment:
  staging:
    branch: master
    heroku:
      appname: semu